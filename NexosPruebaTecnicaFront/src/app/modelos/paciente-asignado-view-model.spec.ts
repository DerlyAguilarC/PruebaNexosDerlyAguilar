import { PacienteAsignadoViewModel } from './paciente-asignado-view-model';

describe('PacienteAsignadoViewModel', () => {
  it('should create an instance', () => {
    expect(new PacienteAsignadoViewModel()).toBeTruthy();
  });
});
